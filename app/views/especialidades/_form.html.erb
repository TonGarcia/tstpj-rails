<%= form_with(model: especialidade, local: true) do |form| %>
  <% if especialidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(especialidade.errors.count, "error") %> prohibited this especialidade from being saved:</h2>

      <ul>
        <% especialidade.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :ativo %>
    <%= form.check_box :ativo %>
  </div>

  <div class="field">
    <%= form.label :localidade %>
    <%= form.text_field :localidade %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
